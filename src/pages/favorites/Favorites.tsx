import { getFavorites } from "../../api/favs"
import type { ImageRecord } from "../../types"

export default function Favorites() {
	/*
  getFavorites().then((favs: ImageRecord) => {
    console.log("favs", favs)
  })
  */
	return <div>Favorites</div>
}
